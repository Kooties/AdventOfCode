$rawData = Get-Content testinput.txt
$maxSize = 100000
$fileRegex = '[\d]{1,}'
$dname = ""
$numUp = 0
$currentDir = ""
foreach($line in $rawData){
    $split = $line.split(' ')
    switch -wildcard ($line){
        
    }
}






$directory.clear()
$cacheList.clear()
$directories.clear()