## Repository guidance for Copilot

- Most solutions are standalone PowerShell scripts organized by year/day (for example `2024/day7/solver.ps1`).
- Prefer small, focused edits that keep each day’s solver self-contained.
- Keep input/test data files (`input.txt`, `example.txt`, `test*.txt`) alongside the solver script for that day.
